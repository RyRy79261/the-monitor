{
  "name": "the-monitor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Live 3D configurator and price estimator for a walk-in CRT-shaped wooden lounge structure (AfrikaBurn proposal).",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.9"
  }
}
